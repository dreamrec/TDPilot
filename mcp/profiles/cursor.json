{
  "$comment": "Cursor MCP profile template. Write this into your Cursor MCP config location.",
  "mcpServers": {
    "touchdesigner": {
      "command": "npx",
      "args": ["-y", "tdpilot"],
      "env": {
        "TD_MCP_HOST": "127.0.0.1",
        "TD_MCP_PORT": "9981",
        "TD_MCP_WS_PORT": "9982"
      }
    }
  }
}
