# v1.5.3 development session — bootstrap prompt

**Paste the block below verbatim into a fresh Claude Code chat to start v1.5.3 dev.** The session won't have memory of how v1.5.2 wrapped up — this prompt makes it self-contained.

---

```
You are continuing TDPilot development for v1.5.3.

The plan is at:
  docs/superpowers/plans/2026-04-25-v1.5.3-plan.md

Read it end-to-end before doing anything. Do NOT skip the "Per-commit
discipline" section.

Current state (verify, don't trust):
- Repo:    /Users/visansilviugeorge/Desktop/DREAM AI/TDPilot/TDPilot-main
- main HEAD: should be f251b30 or later
- Last released: v1.5.2 (2026-04-25) — npm registry + GitHub Release
  + .mcpb + .plugin all live
- Current TD-side .tox: API_VERSION 1.5.2, hash 2d47ba108304fe2a...
- npm: tdpilot@1.5.2 (OIDC-published with provenance)

Memory file (READ THIS — it's project context, not optional):
  ~/.claude/projects/-Users-visansilviugeorge-Desktop-DREAM-AI-TDPilot-TDPilot-main/memory/tdpilot_update_checklist.md

Critical traps to avoid (full detail in the memory file's
"Live-debug pitfalls" section):

  A) Don't move `bootstrap_auth()` back inside `main()` in server.py.
     The v1.5.2 commit 8e8a598 hoisted it above the imports for a
     reason — TD_SHARED_SECRET is captured at module-load. Item 1
     of the plan is the regression test for this.

  B) ~/.tdpilot/ venv installs non-editable by default. To see your
     edits in the running server, `cd ~/.tdpilot && uv pip install
     -e .` (and same for the local-desktop-app-uploads plugin path
     if you debug there).

  C) REQUIRE_AUTH=0 does NOT bypass auth when a secret is loaded.
     Read `_check_auth_error` in mcp_webserver_callbacks.py before
     spending time on any auth puzzle.

  D) ~/.tdpilot/tdpilot_default.toe should already be renamed to
     .bak from the v1.5.2 cleanup. If you see "[TDPilot] .tox not
     found at .../tdpilot_v1_3.tox" in TD Textport, the .toe came
     back from somewhere — re-rename it.

Approach:

1. Run `git status && git log --oneline | head -5` to confirm the
   working tree is clean and main is where the plan expects it.
2. Read docs/superpowers/plans/2026-04-25-v1.5.3-plan.md fully.
3. State which item you're tackling first (default: item 1, the
   regression test — it's the smallest and unblocks confidence in
   subsequent refactors).
4. For each item, follow Per-commit discipline strictly:
   - Write tests first (use superpowers:test-driven-development if
     you're not sure how to structure them)
   - Run all 5 CI gates locally before each commit (pytest, ruff
     check, ruff format, check_versions, check_tox_freshness,
     check_no_personal_paths — six commands listed in the plan)
   - One conventional commit per item with a body that explains
     WHY, referencing the plan path
   - Don't touch td_component/*.py unless explicitly required —
     it forces a TD-side .tox rebuild via Textport which gates on
     the user being available with TD open
5. Update CHANGELOG.md's "Unreleased" section as commits land. Do
   NOT rename it to "1.5.3 - YYYY-MM-DD" — that's part of the
   release flow, which only runs on explicit instruction.

Hard rules:

- Do NOT cut a v1.5.3 release. The plan has a release flow at the
  bottom — it only runs when the user explicitly says "release v1.5.3"
  or equivalent. Ship the work to main; the release is a separate decision.
- Do NOT bump version fields. Version stays at 1.5.2 until release time.
- Do NOT skip CI gates. Use --no-verify only if a gate misfires AND
  you've explained why in the commit body. Default is gates run.
- If something doesn't go as planned, use systematic-debugging skill
  before throwing fixes at it. Phase 1 (root cause) before Phase 4
  (implement).
- If 3 fix attempts fail in a row, STOP. The architecture is wrong;
  ask the user before continuing.

When all in-scope items are done (per the "Definition of v1.5.3 done"
checklist), stop and report status. Do not auto-trigger release.

Begin.
```

---

## Notes for the user (don't include in the pasted block)

- The prompt above is self-contained: a fresh session will know what files to read and what discipline to apply.
- If you want to scope smaller for a particular session ("just do item 1 and item 4"), modify the "Approach" step 3 in the prompt before pasting.
- Feel free to add session-specific context above the code block (e.g., "I have TD open" or "no TD access today, skip anything that needs the .tox rebuild").
